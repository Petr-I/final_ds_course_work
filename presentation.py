import streamlit as st


def presentation_page():
    st.title("🎯 Презентация проекта: Предиктивное обслуживание оборудования")

    with st.expander("## 1. Введение", expanded=True):
        st.markdown("""
        **Цель проекта:**
        🔍 Разработать ML-модель для прогнозирования отказов оборудования
        ⚙️ Предсказать отказ оборудования
        """)

    with st.expander("## 2. Описание данных"):
        st.markdown("""
        **Источник:** AI4I 2020 Predictive Maintenance Dataset (10,000 записей)

        **Ключевые признаки:**
        • Температура процесса: 300-310 K
        • Скорость вращения: 1,160-2,880 об/мин
        • Износ инструмента: 0-253 мин

        **Типы отказов:**
        TWF (Tool Wear) - 120 случаев
        HDF (Heat Dissipation) - 115 случаев
        PWF (Power Failure) - 95 случаев
        """)

    with st.expander("## 3. Моделирование"):
        st.markdown("""
        **Архитектура решения:**
        ```mermaid
            A[Сырые данные] --> B{Предобработка}
            B --> C[Обучение модели]
            C --> D[Прогнозирование]
        ```

        **Выбор модели:**
        • RandomForestClassifier
        • Параметры:
          ```python
          n_estimators=100, random_state=42
          ```
        """)

    with st.expander("## 4. Результаты"):
        col1, col2 = st.columns(2)
        with col1:
            st.markdown("**Метрики:**")
            st.metric("Accuracy", "0.98")
            st.metric("ROC-AUC", "0.96")

    with st.expander("## 5. Демонстрация системы"):
        st.markdown("""
        **Функционал приложения:**
        ✅ Загрузка данных в реальном времени
        ✅ Интерактивные прогнозы
        ✅ Визуализация метрик

        **Пример использования:**
        ```
        Входные параметры:
        Тип = L
        Температура воздуха = 300
        Температура процесса = 310
        Скорость вращения = 1500
        Крутящий момент = 40
        Износ инструмента = 100
        
        
        Результат предсказания:
        Оборудование работает нормально

        Вероятность отказа: 0.00
        ```
        """)

    with st.expander("## 6. Заключение"):
        st.markdown("""
        ✓ Была построена модель предсказывающая отказ оборудования
        """)